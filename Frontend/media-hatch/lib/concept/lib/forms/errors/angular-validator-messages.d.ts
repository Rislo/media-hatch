import { FormControl } from '@angular/forms';
export declare function getRequiredError(control: FormControl): string | null;
export declare function getEmailError(control: FormControl): string | null;
